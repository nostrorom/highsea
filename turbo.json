{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"sync": {},
		"build": {
			"dependsOn": ["^build", "sync"],
			"outputs": ["dist/**"]
		},
		"dev": {
			"dependsOn": ["^build"]
		},
		"format": {},
		"lint": {}
	}
}
